fastapi==0.136.3
uvicorn[standard]==0.49.0
python-multipart==0.0.32

# Stem separation model + audio I/O. Installed locally for real separation.
# (Heavy: pulls in the torch ML stack.)
torch==2.12.0
torchaudio==2.11.0
demucs==4.0.1
soundfile==0.14.0
numpy==2.4.6
# Provides CA certificates for the one-time model weight download on macOS
# (python.org Python ships without them). Point SSL_CERT_FILE at it if needed.
certifi==2025.10.5

# Test dependencies
pytest==8.3.4
httpx==0.28.1
