# Configurable Demucs_Service endpoint (Req 12.3).
# The Frontend reaches the stem separation backend through this URL so the two
# components can be hosted and deployed independently.
NEXT_PUBLIC_DEMUCS_ENDPOINT=http://localhost:8000
